local OutgoingProfanityFilter, NS = ...

local replacementString = "*"

local wordsToReplaceOverrides = {["idiot"] = "chicken"}

NS.RP.replacementString = replacementString
NS.RP.wordsToReplaceOverrides = wordsToReplaceOverrides
